import { SearchPlantsPipe } from './search-plants.pipe';

describe('SearchPlantsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPlantsPipe();
    expect(pipe).toBeTruthy();
  });
});
