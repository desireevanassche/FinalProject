import { FilterLightReqPipe } from './filter-light-req.pipe';

describe('FilterLightReqPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLightReqPipe();
    expect(pipe).toBeTruthy();
  });
});
