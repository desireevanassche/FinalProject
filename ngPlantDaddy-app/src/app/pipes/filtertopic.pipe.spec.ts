import { FilterTopicPipe } from './filter-topic.pipe';

describe('FilterTopicPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTopicPipe();
    expect(pipe).toBeTruthy();
  });
});
