import { WatercyclefilterPipe } from './watercyclefilter.pipe';

describe('WatercyclefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WatercyclefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
