import { WatertypefilterPipe } from './watertypefilter.pipe';

describe('WatertypefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WatertypefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
